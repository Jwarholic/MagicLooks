<%= render 'partials/header' %>
<h1> Profile Page </h1>

Hello <%= @user.name.capitalize %>.

<%= link_to 'New Mirror', new_mirror_path, class: 'new-mirror', remote: true %>
<div class='new-mirror'>
  <%= link_to 'New Mirror User', users_mirrors_path(@path),action: 'index', remote: true %>
</div>

<div class='new-mirrors'></div>
<div class='add_mirror_user'></div>
<%= link_to "Edit", edit_user_path(@user) %>
